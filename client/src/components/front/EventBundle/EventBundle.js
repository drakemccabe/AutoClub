import React, { Component } from 'react';
import { formatDate } from "../../../helpers";

class EventBundle extends Component {

  render() {

    const { details} = this.props;

    return (
      <div>

      </div>
    );
  }
}

export default EventBundle;
